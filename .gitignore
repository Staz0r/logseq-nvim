# Generated / build output (regenerated by CI + every clone — never commit)
dist/
coverage/
*.rock
*.rockspec

# Local env / secrets — NEVER commit
.env
.env.*
*.local

# OS / editor
.DS_Store
.idea/
.vscode/

# CodeGraph index (machine-local; regenerate with `codegraph init -i`)
.codegraph/
